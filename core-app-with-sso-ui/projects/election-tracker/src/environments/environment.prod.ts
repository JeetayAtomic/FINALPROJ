export const environment = {
  production: true,
  // In production the SPA and API are served behind the same host, so '/api/...' is relative.
  apiBaseUrl: '',
  loginUrl: 'http://localhost:4200/login',
};
